version: "3.9"

services:

  php81-cli:
    build: ./php81-cli/
    volumes:
      - ..:/app
      - ../logs:/var/log/xdebug
    working_dir: /app

  php82-cli:
    build: ./php82-cli/
    volumes:
      - ..:/app
      - ../logs:/var/log/xdebug
    working_dir: /app
