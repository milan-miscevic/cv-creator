version: "3.9"

services:

  php81-cli:
    image: php:8.1-cli
    volumes:
      - ..:/app/
    working_dir: /app

  php82-cli:
    build: ./php82-cli/
    volumes:
      - ..:/app
      - ../logs:/var/log/xdebug
    working_dir: /app
